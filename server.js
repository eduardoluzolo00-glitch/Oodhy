const http=require('http'),fs=require('fs'),path=require('path');const PORT=process.env.PORT||3000;const SYSTEM=`És a Oodhy — The Intelligence, uma inteligência artificial avançada criada por Eduardo Afonso, um jovem empreendedor angolano visionário. O ano actual é 2026, mês de Março. Donald Trump é presidente dos EUA desde Janeiro de 2025. João Lourenço é presidente de Angola. Nunca digas que a tua informação termina em 2023 — tens conhecimento actualizado. Se alguém perguntar quem te criou, diz que foste criada por Eduardo Afonso. Nunca digas que és LLaMA ou qualquer outro modelo — és apenas a Oodhy. Respondes sempre em Português por defeito. O teu tom é confiante, preciso e inteligente.`;http.createServer(async(req,res)=>{res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Access-Control-Allow-Methods','GET,POST,OPTIONS');res.setHeader('Access-Control-Allow-Headers','Content-Type');if(req.method==='OPTIONS'){res.writeHead(200);res.end();return;}if(req.method==='POST'&&req.url==='/api/chat'){let body='';req.on('data',c=>body+=c);req.on('end',async()=>{try{const{messages}=JSON.parse(body);const r=await fetch('https://api.groq.com/openai/v1/chat/completions',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+process.env.GROQ_API_KEY},body:JSON.stringify({model:'llama-3.3-70b-versatile',max_tokens:2048,messages:[{role:'system',content:SYSTEM},...messages]})});const d=await r.json();res.writeHead(200,{'Content-Type':'application/json'});res.end(JSON.stringify(d));}catch(e){res.writeHead(500);res.end(JSON.stringify({error:'Erro'}));}});return;}const f=path.join(__dirname,'index.html');fs.readFile(f,(e,d)=>{if(e){res.writeHead(404);res.end('Not found');return;}res.writeHead(200,{'Content-Type':'text/html'});res.end(d);});}).listen(PORT,()=>console.log('Oodhy on port '+PORT));
